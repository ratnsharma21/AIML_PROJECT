# Python Decision Making - Reference Guide

This folder contains Python exercises focusing on conditional statements (`if`, `elif`, `else`), nested decision making, and logical operators (`and`, `or`, `not`). 

Each concept is implemented in its own subfolder as a standalone executable script. 

