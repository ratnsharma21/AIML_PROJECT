# Python Basic Functions - Reference Guide

This folder contains Python exercises focusing on function definitions, arguments, return values, variable scopes, recursion, and higher-order built-in functions (`map`, `filter`, `reduce`).

Each concept is implemented in its own subfolder as a standalone executable script. 